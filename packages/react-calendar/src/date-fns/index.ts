/*
 * Public API surface of @ascentsparksoftware/react-calendar/date-fns —
 * the default timezone-correct date engine built on date-fns(+tz).
 */
export { DateFnsDateAdapter } from './date-fns-adapter';
